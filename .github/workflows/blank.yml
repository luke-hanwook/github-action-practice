name: CI
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Run pwd
        run: pwd

      - name: Run touch
        run: touch test.txt

      - name: Run ls -al
        run: ls -al
